<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/myriad</item>
        <item name="android:editTextColor">@color/buttonBackground</item>
        <item name="android:textColorHint">@color/greyTxt</item>
        <item name="android:textColorSecondary">@color/greyTxt</item>
    </style>

</resources>
